{
  "name": "bloi",
  "version": "1.0.0",
  "description": "Turn Invoices into Yield. Automatically. Built for Lepton Agents Hackathon on Base Sepolia.",
  "private": true,
  "workspaces": [
    "app",
    "agent",
    "contracts"
  ],
  "scripts": {
    "dev": "pnpm --parallel run dev",
    "dev:app": "pnpm --filter bloi-app dev",
    "dev:agent": "pnpm --filter bloi-agent dev",
    "build": "pnpm --parallel run build",
    "build:contracts": "npm --prefix contracts run build",
    "docker:build:agent": "docker build -f Dockerfile.mcp -t bloi-agent .",
    "docker:build": "pnpm run docker:build:agent",
    "test": "pnpm test:all",
    "test:all": "pnpm test:contracts && pnpm test:agent",
    "test:contracts": "npm --prefix contracts run test",
    "test:agent": "pnpm --filter bloi-agent test",
    "typecheck": "pnpm --filter bloi-app tsc --noEmit && pnpm --filter bloi-agent tsc --noEmit",
    "lint": "pnpm --filter bloi-app lint && npm --prefix contracts run build",
    "deploy:local": "npm --prefix contracts run deploy:local",
    "deploy:factory": "npm --prefix contracts run deploy:factory",
    "deploy:base-sepolia": "npm --prefix contracts run deploy:base-sepolia",
    "verify:base-sepolia": "npm --prefix contracts run verify:base-sepolia"
  },
  "devDependencies": {
    "puppeteer": "^24.34.0"
  }
}
